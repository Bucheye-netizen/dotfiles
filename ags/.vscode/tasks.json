{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "AGS Build",
            "type": "shell",
            // Assign output file name with VSCode inner variables like ${fileBasename}
            "command": "ags -c /home/lisan/.config/nixos/ags/config.js",
            "options": {},
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}